# Generated by `pnpm deploy:sync`. Edit `deploy/config.ts`, then re-run sync.
# Dokploy: Compose path ./docker-compose.yml — set dokploy.domain in deploy/config.ts then re-sync.

services:
  better-auth-starterkit:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - BETTER_AUTH_URL=${BETTER_AUTH_URL}
      - PORT=3000
      - HOSTNAME=0.0.0.0
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.better-auth-starterkit.rule=Host(`auth.example.com`)"
      - "traefik.http.routers.better-auth-starterkit.entrypoints=websecure"
      - "traefik.http.routers.better-auth-starterkit.tls.certResolver=letsencrypt"
      - "traefik.http.services.better-auth-starterkit.loadbalancer.server.port=3000"

networks:
  dokploy-network:
    external: true
