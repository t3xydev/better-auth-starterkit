/** In-app Fumadocs site at `/docs/framework`. Opt-in; set `NEXT_PUBLIC_DOCS_ENABLED=true` to enable. */
export const docsEnabled = process.env.NEXT_PUBLIC_DOCS_ENABLED === "true"

export const docsBasePath = "/docs/framework"
